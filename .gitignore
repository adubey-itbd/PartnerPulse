# Secrets — never commit; use Secret Manager on Firebase
.env
*.env

# Generated artifacts — the whole directory (per-partner caches, downloaded
# decks, run logs). Nothing in data/ is ever tracked; scripts go in scripts/.
/data/

# Python
__pycache__/
*.pyc
.venv/
venv/
