# PartnerPulse extraction + AI engine
requests>=2.31
markitdown[pdf,docx,pptx]>=0.1.0   # .docx transcripts + PDF/PPTX deck conversion
openai>=1.40                        # AI churn analysis — Grok via Azure AI Foundry (OpenAI SDK, base_url + key)
python-dotenv>=1.0                  # load .env for local runs
